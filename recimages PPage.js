
    //Getting Images

    let string = 'HALLO';

    const capitalizeFirstLetter = (string) => {
        return string[0].toUpperCase() + string.slice(1);
    }

    console.log(capitalizeFirstLetter(string.toLowerCase()));
